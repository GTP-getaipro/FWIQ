# SendGrid Credentials for FloWorx
# This is a TEMPLATE file - Coolify injects real values at runtime
# DO NOT put real credentials here - use Coolify environment variables

SENDGRID_API_KEY={{COOLIFY_SENDGRID_API_KEY}}
SENDGRID_FROM_EMAIL=noreply@floworx-iq.com
SENDGRID_FROM_NAME=FloWorx-iq team

SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=465
SMTP_USER=apikey
SMTP_PASS={{COOLIFY_SENDGRID_API_KEY}}

SESSION_SECRET={{COOLIFY_SESSION_SECRET}}
SENTRY_DSN={{COOLIFY_SENTRY_DSN}}

# Instructions:
# 1. In Coolify Dashboard, set environment variables:
#    - SENDGRID_API_KEY: Your SendGrid API key
#    - SESSION_SECRET: Random secure string (32+ characters)
#    - SENTRY_DSN: Your Sentry DSN (optional)
#
# 2. Coolify will inject these values at build/runtime
# 3. Never commit real credentials to git

# For local development:
# Copy this file to .sendgrid-credentials.txt (gitignored)
# Replace placeholders with your local test values

